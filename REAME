vagrant up
vagrant ssh
Access the Flask app at:

    Host Machine: http://localhost:5000/greet?name=Test

# Install dependencies
sudo apt update && sudo apt install -y python3-flask

# Run the vulnerable app
python3 vuln_app.py  # Runs on http://0.0.0.0:5000

# Set up privesc
chmod +x setup.sh
./setup.sh

